#include "stdafx.h"
#include <math.h>

int main(int argc, char* argv[])
{
	ceil(111.44);	// 올림
	floor(111.44);	// 내림
	abs(-14);		// 절댓값
	pow(2, 3);		// 2^3
	sqrt(4);		// root
	exp(4);			// e^4
	log(10);		// ln 10
	log10(10);		// log10(10)
	sin();
	cos();
	tan();
	asin();
	acos();
	atan();

	return 0;
}